# Points to log when the vehicle approaches them.
# Distance threshold is in meters.
logging_points:
  default_threshold: 2.0
  points:
    # - name: "P1"
    #   x: 0.0
    #   y: 0.0
    #   z: 0.0
    #   threshold: 2.0
